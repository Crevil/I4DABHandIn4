<Window x:Class="GUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:oxy="http://oxyplot.org/wpf"
        Title="Grundfos Dormitory Lab" Height="486.397" Width="728.309">
    <Grid>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"></ColumnDefinition>
            <ColumnDefinition Width="Auto"></ColumnDefinition>
            <ColumnDefinition Width="1*"></ColumnDefinition>
            <ColumnDefinition Width="Auto"></ColumnDefinition>
            <ColumnDefinition Width="4*"></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <GridSplitter Grid.Column="1"
                      HorizontalAlignment="Center" 
                      ShowsPreview="true" VerticalAlignment="Stretch" 
                      Width="2"/>

        <GridSplitter Grid.Column="3"
                      HorizontalAlignment="Center" 
                      ShowsPreview="true" VerticalAlignment="Stretch" 
                      Width="2"/>
        
        <ListBox x:Name="LocationsListBox" 
                 ItemsSource="{Binding Locations}"
                 DisplayMemberPath="Name"
                 Grid.Column="0"
                 Margin="10">
        </ListBox>

        <ListBox x:Name="SensorListBox"
                 ItemsSource="{Binding Sensors}"
                 DisplayMemberPath="Name"
                 Grid.Column="2"
                 Margin="10">
        </ListBox>

        <oxy:Plot Grid.Column="4" x:Name="GraphPlot" Title="Test" Model="{Binding Graph.PlotModel}" Margin="10">
        </oxy:Plot>
    </Grid>
</Window>
